{
  "name": "@ssr-themes/svelte-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev --port 4046",
    "build": "vite build",
    "preview": "vite preview",
    "check-types": "bun run check",
    "test": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "clean": "rm -rf .svelte-kit build node_modules"
  },
  "dependencies": {
    "ssr-themes": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^7.0.1",
    "@sveltejs/kit": "^2.48.0",
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tailwindcss/vite": "^4.2.2",
    "@types/node": "^25.5.0",
    "svelte": "^5.40.1",
    "svelte-check": "^4.3.1",
    "tailwindcss": "^4.2.2",
    "typescript": "^6.0.2",
    "vite": "^8.0.3"
  }
}
